const app=document.getElementById('app');
const state={
  lang:localStorage.getItem('cc_lang')||'en',page:'home',player:null,questions:[],index:0,answers:{},game:null,feedback:null,countries:[],finalResult:null,finalAttempt:null,rankingTimer:null
};

const T={
  en:{brand:'CYBER CHALLENGE',tag:'Play → Learn → Improve → Prove',start:'Start Challenge',language:'Language',playerInfo:'Player Information',first:'First Name',last:'Last Name',age:'Age',country:'Country',selectCountry:'Select your country',continue:'Continue',initial:'Initial Cyber Awareness Test',submit:'Submit Answer',next:'Next',finish:'Finish Test',initialScore:'Your Initial Awareness Score',journey:'Now your Cyber Challenge journey begins.',level:'Level',challenge:'Challenge',completed:'Challenges Completed',xp:'XP',rank:'Rank',badges:'Badges',correct:'Correct',wrong:'Not the safest choice',nextChallenge:'Next Challenge',final:'Final Cyber Awareness Test',progress:'Your Progress',before:'Before',after:'After',improvement:'Improvement',certificateReady:'Certificate Ready',download:'Download Certificate',verify:'Verify Certificate',review:'You haven’t reached the certification score yet.',retry:'Retry Final Test',privacy:'We store only the minimum information needed for the challenge. Do not enter sensitive information.',home:'Home',back:'Back',noCert:'Certificate not available yet.',verifyHint:'Enter Certificate ID',verifyBtn:'Verify',valid:'Certificate verified',invalid:'Certificate not found',welcome:'Learn. Challenge Yourself. Stay Safe.',gameIntro:'Short realistic scenarios. Think before you click.',admin:'Admin Dashboard',rankings:'Live Rankings',live:'LIVE • updates every second',world:'World',continent:'Continent',countryBoard:'Country',top3:'Top 3',champion:'Champion',elite:'Elite',worldChampion:'World Cyber Champion',continentChampion:'Continental Cyber Champion',nationalChampion:'National Cyber Champion',worldElite:'World Elite',continentElite:'Continental Elite',nationalElite:'National Elite',owner:'Owned & Founded by Douba Jouad',wrongEffect:'THREAT DETECTED',correctEffect:'SECURITY VERIFIED',saved:'Answer locked',scenario:'Live scenario',browser:'Browser',email:'Email',chat:'WhatsApp',bank:'Banking app',phone:'Phone call',permissions:'Permissions',calendar:'Calendar',market:'Marketplace',profile:'Public profile',cloud:'Cloud storage',video:'Video call',job:'Recruitment chat',community:'Community',shop:'Shopping app',contacts:'Contacts',location:'Precise location',microphone:'Microphone',recovery:'Recovery email',password:'Password',verification:'Verification code',sender:'Sender',recipient:'Recipient',subject:'Subject',reply:'Reply',attachment:'Attachment',openApp:'Open app directly',officialSite:'Official site',domain:'Domain',unexpected:'Unexpected',safeRoute:'Trusted route'},
  ar:{brand:'التحدي السيبراني',tag:'العب ← تعلّم ← طوّر ← أثبت',start:'ابدأ التحدي',language:'اللغة',playerInfo:'بيانات اللاعب',first:'الاسم الأول',last:'اسم العائلة',age:'العمر',country:'الدولة',selectCountry:'اختر دولتك',continue:'متابعة',initial:'اختبار الوعي السيبراني الأولي',submit:'إرسال الإجابة',next:'التالي',finish:'إنهاء الاختبار',initialScore:'نتيجة الوعي الأولية',journey:'الآن تبدأ رحلتك في Cyber Challenge.',level:'المستوى',challenge:'التحدي',completed:'التحديات المكتملة',xp:'XP',rank:'الرتبة',badges:'الشارات',correct:'إجابة صحيحة',wrong:'ليست الخيار الأكثر أمانًا',nextChallenge:'التحدي التالي',final:'اختبار الوعي السيبراني النهائي',progress:'تقدمك',before:'قبل',after:'بعد',improvement:'التحسن',certificateReady:'الشهادة جاهزة',download:'تحميل الشهادة',verify:'التحقق من الشهادة',review:'لم تصل بعد إلى درجة الاعتماد المطلوبة.',retry:'إعادة الاختبار النهائي',privacy:'نخزن الحد الأدنى من المعلومات اللازمة للتحدي فقط. لا تدخل معلومات حساسة.',home:'الرئيسية',back:'رجوع',noCert:'الشهادة غير متاحة بعد.',verifyHint:'أدخل معرّف الشهادة',verifyBtn:'تحقق',valid:'تم التحقق من الشهادة',invalid:'لم يتم العثور على الشهادة',welcome:'تعلّم. تحدَّ نفسك. ابقَ آمنًا.',gameIntro:'سيناريوهات قصيرة وواقعية. فكّر قبل أن تضغط.',admin:'لوحة المشرف',rankings:'الترتيب المباشر',live:'مباشر • يتحدث كل ثانية',world:'العالم',continent:'القارة',countryBoard:'الدولة',top3:'أفضل 3',champion:'البطل',elite:'النخبة',worldChampion:'بطل العالم السيبراني',continentChampion:'بطل القارة السيبراني',nationalChampion:'بطل الدولة السيبراني',worldElite:'نخبة العالم',continentElite:'نخبة القارة',nationalElite:'نخبة الدولة',owner:'المالك والمؤسس: دوبة جواد',wrongEffect:'تم رصد تهديد',correctEffect:'تم التحقق أمنيًا',saved:'تم تثبيت الإجابة',scenario:'موقف حي',browser:'متصفح',email:'بريد إلكتروني',chat:'واتساب',bank:'تطبيق بنكي',phone:'مكالمة',permissions:'الصلاحيات',calendar:'التقويم',market:'سوق إلكتروني',profile:'ملف عام',cloud:'تخزين سحابي',video:'مكالمة فيديو',job:'محادثة توظيف',community:'مجتمع',shop:'تطبيق تسوق',contacts:'جهات الاتصال',location:'الموقع الدقيق',microphone:'الميكروفون',recovery:'بريد الاسترداد',password:'كلمة المرور',verification:'رمز التحقق',sender:'المرسل',recipient:'المستلم',subject:'الموضوع',reply:'رد',attachment:'مرفق',openApp:'فتح التطبيق مباشرة',officialSite:'الموقع الرسمي',domain:'النطاق',unexpected:'غير متوقع',safeRoute:'مسار موثوق'}
};
const t=k=>T[state.lang][k]||k;
const L=(en,ar)=>state.lang==='ar'?ar:en;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const dir=()=>state.lang==='ar'?'rtl':'ltr';
const pageStack=[];
let suppressNextPop=false;
function syncHistory(page,replace=false){try{const base=location.href.split('#')[0];const url=page==='home'?base:`${base}#${encodeURIComponent(page)}`;(replace?history.replaceState:history.pushState).call(history,{ccPage:page},'',url)}catch{}}
try{
  const initialHash=decodeURIComponent(location.hash.replace(/^#/,'')).trim();
  const initialPage=['home','info','initial','initialResult','game','finalIntro','final','progress','certificate','rankings'].includes(initialHash)?initialHash:'home';
  state.page=initialPage;
  history.replaceState({ccPage:initialPage},'',initialPage==='home'?location.href.split('#')[0]:location.href);
}catch{}
function navigate(page,{replace=false,force=false,history=true}={}){
  if(page===state.page && !force)return;
  if(page!==state.page) pageStack.push(state.page);
  state.page=page;
  if(history) syncHistory(page,replace);
  render();
}
function goBack(){
  cleanup();
  const previous=pageStack.pop()||'home';
  state.page=previous;
  syncHistory(previous,true);
  render();
}
window.addEventListener('popstate',e=>{
  cleanup();
  const page=e.state?.ccPage || decodeURIComponent(location.hash.replace(/^#/,'')).trim() || 'home';
  state.page=['home','info','initial','initialResult','game','finalIntro','final','progress','certificate','rankings'].includes(page)?page:'home';
  pageStack.length=0;
  if(suppressNextPop)suppressNextPop=false;
  render();
});

function cleanup(){if(state.rankingTimer){clearInterval(state.rankingTimer);state.rankingTimer=null}}
function header(){return `<header class="topbar"><div class="brand"><div class="brand-mark">⌬</div><div>${esc(t('brand'))}<small>${esc(t('tag'))}</small></div></div><div class="top-actions">${state.page!=='home'?`<button class="nav-link back-link" id="backNav">← ${esc(t('back'))}</button>`:''}<button class="nav-link ${state.page==='rankings'?'active':''}" id="rankNav">🏆 ${esc(t('rankings'))}</button><div class="lang-switch"><button class="${state.lang==='ar'?'active':''}" data-lang="ar">العربية</button><button class="${state.lang==='en'?'active':''}" data-lang="en">English</button></div></div></header>`}
function shell(content,cls=''){cleanup();document.documentElement.lang=state.lang;document.body.className=dir();app.innerHTML=`<div class="app-shell ${dir()} ${cls}">${header()}<main class="page">${content}</main><div class="footer"><span>${esc(t('privacy'))}</span><strong>${esc(t('owner'))}</strong></div></div>`;bindCommon()}
function bindCommon(){bindFlagFallbacks();document.querySelectorAll('[data-lang]').forEach(b=>b.onclick=()=>{state.lang=b.dataset.lang;localStorage.setItem('cc_lang',state.lang);render()});const rank=document.getElementById('rankNav');if(rank)rank.onclick=()=>navigate('rankings');const back=document.getElementById('backNav');if(back)back.onclick=()=>goBack()}
function effect(type){const el=document.createElement('div');el.className=`fx-overlay ${type==='correct'?'fx-secure':'fx-hack'}`;el.innerHTML=`<div class="fx-grid"></div><div class="fx-core">${type==='correct'?'🛡️':'⚠️'}</div><div class="fx-text">${esc(type==='correct'?t('correctEffect'):t('wrongEffect'))}</div>`;document.body.appendChild(el);setTimeout(()=>el.classList.add('show'),10);setTimeout(()=>el.remove(),1000);if(type==='wrong')document.body.classList.add('screen-shake'),setTimeout(()=>document.body.classList.remove('screen-shake'),420)}
function flashSaved(){const e=document.createElement('div');e.className='saved-toast';e.textContent=t('saved');document.body.appendChild(e);setTimeout(()=>e.remove(),1100)}

function home(){shell(`<section class="hero"><div class="hero-copy"><div class="eyebrow">Cybersecurity Awareness Game</div><h1>CYBER<br><span>CHALLENGE</span></h1><p>${esc(t('welcome'))}<br>${esc(t('gameIntro'))}</p><div class="hero-badges"><span>📩 Phishing</span><span>🎭 Scams</span><span>🔐 Accounts</span><span>🕵️ Privacy</span></div><div class="cta"><button class="btn btn-primary" id="start">${esc(t('start'))}</button><a class="btn btn-secondary" href="verify.html">${esc(t('verify'))}</a></div></div><div class="hero-card"><div class="hud"><span>LIVE DEFENSE MODE</span><span>●</span></div><div class="shield">🛡️</div><h2>Play → Learn → Improve</h2><p>${4} levels · ${CC_DATA.challenges.length} scenarios · 2 tests · XP · badges · certification</p><div class="hero-points"><div><strong>100</strong><span>point tests</span></div><div><strong>70+</strong><span>certificate score</span></div><div><strong>${CC_DATA.challenges.length}</strong><span>game challenges</span></div><div><strong>2</strong><span>languages</span></div></div><div class="scanline"></div></div></section><section class="owner-strip"><span>⌁ ${esc(t('owner'))}</span><a href="admin.html">${esc(t('admin'))}</a></section>`, 'home-page');document.getElementById('start').onclick=()=>navigate('info')}

async function loadCountries(){state.countries=CC_DATA.countries||[]}
function flagImg(c){const code=String(c?.code||'').toUpperCase();const emoji=esc(c?.flag||'🏳️');if(!code)return `<span class="country-flag-fallback">${emoji}</span>`;const remote=`https://flagcdn.com/w40/${code.toLowerCase()}.png`;return `<img class="country-flag" src="${remote}" alt="" width="28" height="19" loading="lazy" onerror="this.hidden=true;this.nextElementSibling.hidden=false"><span class="country-flag-fallback" hidden>${emoji}</span>`}
function bindFlagFallbacks(root=document){}
function countryOptions(){return state.countries.map(c=>`<button type="button" class="country-option" data-country="${esc(c.code)}"><span class="country-option-flag">${flagImg(c)}</span><span>${esc(c[state.lang])}</span><small>${esc(c.code)}</small></button>`).join('')}
function setupCountryPicker(){const root=document.getElementById('countryPicker');if(!root)return;const hidden=document.getElementById('country');const trigger=document.getElementById('countryTrigger');const menu=document.getElementById('countryMenu');const valueLabel=document.getElementById('countryValue');const setCountry=code=>{const c=state.countries.find(x=>x.code===code);if(!c)return;hidden.value=c.code;valueLabel.innerHTML=`${flagImg(c)}<span>${esc(c[state.lang])}</span>`;trigger.classList.add('has-value');menu.hidden=true};trigger.onclick=e=>{e.stopPropagation();menu.hidden=!menu.hidden};root.querySelectorAll('[data-country]').forEach(btn=>btn.onclick=()=>setCountry(btn.dataset.country));return setCountry}
document.addEventListener('click',()=>{const menu=document.getElementById('countryMenu');if(menu)menu.hidden=true});
function info(){shell(`<div class="card form-card info-card"><div class="eyebrow">${esc(t('playerInfo'))}</div><h2 class="section-title">${esc(t('playerInfo'))}</h2><p class="section-subtitle">${esc(t('privacy'))}</p><form id="infoForm" class="form-grid" style="margin-top:20px"><div class="field"><label>${esc(t('first'))}</label><input id="first" maxlength="40" required></div><div class="field"><label>${esc(t('last'))}</label><input id="last" maxlength="50" required></div><div class="field"><label>${esc(t('age'))}</label><input id="age" type="number" min="10" max="99" required></div><div class="field"><label>${esc(t('country'))}</label><div class="country-picker" id="countryPicker"><input type="hidden" id="country"><button type="button" class="country-trigger" id="countryTrigger"><span id="countryValue"><span class="country-placeholder">${esc(t('selectCountry'))}</span></span><span>⌄</span></button><div class="country-menu" id="countryMenu" hidden>${countryOptions()}</div></div></div><div class="field full"><button class="btn btn-primary" type="submit">${esc(t('continue'))}</button></div><div id="err" class="notice hidden"></div></form></div>`);if(!state.countries.length)loadCountries().then(()=>{const m=document.getElementById('countryMenu');if(m){m.innerHTML=countryOptions();setupCountryPicker();bindFlagFallbacks(m)}});else setupCountryPicker();bindFlagFallbacks(document.getElementById('countryPicker'));document.getElementById('infoForm').onsubmit=async e=>{e.preventDefault();const err=document.getElementById('err');err.classList.add('hidden');const firstEl=document.getElementById('first'),lastEl=document.getElementById('last'),ageEl=document.getElementById('age'),countryEl=document.getElementById('country');if(!countryEl.value){err.textContent=state.lang==='ar'?'اختر دولتك للمتابعة.':'Select your country to continue.';err.classList.remove('hidden');return}try{const p={id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),firstName:firstEl.value.trim(),lastName:lastEl.value.trim(),age:Number(ageEl.value),country:state.countries.find(c=>c.code===countryEl.value)||state.countries.find(c=>c.code==='DZ'),language:state.lang,initialScore:null,finalScore:null,improvement:null,xp:0,rank:'Cyber Rookie',badges:[],gameProgress:0,certificateId:null,completionDate:null,passed:false};state.player=p;state.questions=CC_DATA.initial[state.lang]||CC_DATA.initial.en;state.questions=shuffleLocal(state.questions);state.answers={};state.index=0;persist();navigate('initial');flashSaved()}catch(x){err.textContent=x.message;err.classList.remove('hidden')}}}

function sceneLabel(scene){
  return ({browser:t('browser'),email:t('email'),cloud:t('cloud'),whatsapp:t('chat'),phone:t('phone'),account:state.lang==='ar'?'الحساب':'Account',app:t('permissions'),social:state.lang==='ar'?'شبكة اجتماعية':'Social',quiz:state.lang==='ar'?'اختبار':'Quiz',market:t('market'),video:t('video'),calendar:t('calendar'),bank:t('bank'),job:t('job'),community:t('community'),profile:t('profile'),shop:t('shop')})[scene]||t('scenario');
}
function sceneText(v){
  if(v==null)return '';
  if(typeof v==='string')return esc(v);
  if(typeof v==='object' && v.en && v.ar)return esc(state.lang==='ar'?v.ar:v.en);
  return esc(v);
}
function visualRow(v){return `<div class="visual-row"><span>${sceneText(v)}</span><b>›</b></div>`}
function sceneMarkup(q){
  const v=q.visual||{}; const s=q.scene||'browser';
  const appName=sceneText(v.app||sceneLabel(s));
  let body='';
  if(v.type==='chat'||s==='whatsapp'||v.type==='class-group'){
    const msgs=(v.messages||[]).map((m,i)=>`<div class="bubble ${i%3===2?'me':'other'}"><b>${i===0?sceneText(v.sender||v.contact||'') : ''}</b>${i===0?'<br>':''}${sceneText(m)}</div>`).join('');
    return `<div class="scene realistic-device chat-real"><div class="device-status">9:41　⌁　▮</div><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>⋮</span></div><div class="chat-meta">${sceneText(v.badge||v.verified||'Online')}</div><div class="chat-body">${msgs}</div></div>`;
  }
  if(v.type==='login') return `<div class="scene realistic-device login-real"><div class="device-status">9:41　▮▮▮</div><div class="scene-top"><span>←</span><strong>${appName}</strong><span>⋮</span></div><div class="urlbar">🔒 ${esc(v.address||'accounts.example')}</div><div class="login-card"><div class="fake-logo">◉</div><h3>${sceneText(v.banner||L('Sign in','تسجيل الدخول'))}</h3><input disabled placeholder="Email"><input disabled placeholder="Password"><div class="helper">${sceneText(v.helper||'')}</div><button class="fake-action">${sceneText(v.action||L('Continue','متابعة'))}</button></div></div>`;
  if(v.type==='delivery') return `<div class="scene realistic-device commerce-real"><div class="device-status">9:41　▮▮▮</div><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>🔔</span></div><div class="order-card"><small>${esc(v.order||'')}</small><h3>${sceneText(v.status||'')}</h3><div class="route-line"><i></i><i></i><i></i></div><div class="payment-alert">${sceneText(v.message||'')}</div><button class="fake-action">${sceneText(v.action||L('Continue','متابعة'))}</button></div></div>`;
  if(v.type==='incoming-call') return `<div class="scene realistic-device call-real"><div class="device-status">9:41　▮▮▮</div><div class="call-head"><div class="caller-icon">☎</div><strong>${sceneText(v.caller||'')}</strong><small>${esc(v.number||'')}</small><span>${sceneText(v.tag||'')}</span></div><div class="call-script"><p>${sceneText(v.script||'')}</p></div><div class="call-buttons"><button>✕</button><button>✓</button></div></div>`;
  if(v.type==='market'||v.type==='invoice') return `<div class="scene realistic-device mail-real"><div class="scene-top"><span>✉</span><strong>${appName}</strong><span>☆</span></div><div class="mail-header"><b>${sceneText(v.sender||v.seller||'')}</b><small>${sceneText(v.subject||v.order||'')}</small></div><div class="mail-sheet"><h3>${sceneText(v.title||L('Refund status','حالة الاسترداد'))}</h3><div class="amount-line"><span>${sceneText(v.expected||v.amount||'')}</span><b>${sceneText(v.alert||v.banner||'')}</b></div><p>${sceneText(v.chat||v.callout||'')}</p></div></div>`;
  if(v.type==='security-center'||v.type==='recovery'||v.type==='social-security'){
    const rows=(v.rows||[]).map(r=>visualRow(r)).join('');
    return `<div class="scene realistic-device security-real"><div class="scene-top"><span>←</span><strong>${appName}</strong><span>⚙</span></div><div class="security-alert">⚠ ${sceneText(v.title||v.alert||L('Security notice','تنبيه أمني'))}</div>${v.newValue?`<div class="identity-box"><small>${sceneText(L('Recovery email','بريد الاسترداد'))}</small><b>${esc(v.newValue)}</b></div>`:''}${rows||visualRow(L('Sessions','الجلسات'))}${visualRow(L('Password','كلمة المرور'))}${visualRow(L('Two-step verification','التحقق بخطوتين'))}</div>`;
  }
  if(v.type==='permissions'){
    return `<div class="scene realistic-device permissions-real"><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>✓</span></div><div class="permission-intro"><b>${sceneText(v.title||L('Before you continue','قبل المتابعة'))}</b><small>${sceneText(v.feature||'')}</small></div>${(v.permissions||[]).map(x=>`<div class="permission-item"><span>${sceneText(x)}</span><b>›</b></div>`).join('')}<div class="fake-action outline">${sceneText(L('Review choices','مراجعة الخيارات'))}</div></div>`;
  }
  if(v.type==='photo-post'||v.type==='social-offer'||v.type==='profile'){
    return `<div class="scene realistic-device social-real"><div class="scene-top"><span>⌂</span><strong>${appName}</strong><span>⋯</span></div><div class="social-profile"><div class="avatar-lg">${esc((v.name||v.account||'S').toString().slice(0,2))}</div><div><b>${esc(v.name||v.account||'Public profile')}</b><small>${esc(v.followers||'')}</small></div><span class="badge-check">✓</span></div><div class="post-card"><div class="post-image"><span>${sceneText(v.offer||v.note||L('Photo preview','معاينة الصورة'))}</span></div><div class="post-caption">{ ${sceneText(v.risk||'')} }</div></div></div>`;
  }
  if(v.type==='student-offer'||v.type==='community-form'){
    return `<div class="scene realistic-device form-real"><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>?</span></div><h3>${sceneText(v.claim||L('Create your profile','أنشئ ملفك'))}</h3>${(v.fields||[]).map(f=>`<div class="form-line"><span>${sceneText(f)}</span><b>▾</b></div>`).join('')}<button class="fake-action">${sceneText(L('Continue','متابعة'))}</button></div>`;
  }
  if(v.type==='document') return `<div class="scene realistic-device document-real"><div class="scene-top"><span>←</span><strong>${appName}</strong><span>⋯</span></div><div class="doc-head"><b>${esc(v.file||'Document.docx')}</b><small>${sceneText(v.note||'')}</small></div><div class="doc-banner">${sceneText(v.banner||'')}</div><div class="doc-page"><h3>Q3 REPORT</h3><p>${state.lang==='ar'?'محتوى المستند':'Document contents'}</p><div class="doc-lines"></div></div><button class="fake-action danger-action">${sceneText(v.button||L('Enable Content','تفعيل المحتوى'))}</button></div>`;
  if(v.type==='wifi') return `<div class="scene realistic-device wifi-real"><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>✓</span></div><div class="wifi-network"><div class="wifi-icon">⌁</div><b>${esc(v.network||'Cafe_WiFi')}</b><small>${sceneText(v.security||'')}</small></div>${visualRow(v.phone||'')}${visualRow(L('Auto-join','الانضمام التلقائي'))}<div class="wifi-bars">▂ ▄ ▆ █</div></div>`;
  if(v.type==='device-reset') return `<div class="scene realistic-device settings-real"><div class="scene-top"><span>‹</span><strong>${appName}</strong><span>⌕</span></div><h3>${sceneText(v.title||L('Before handing over','قبل التسليم'))}</h3>${(v.rows||[]).map(r=>visualRow(r)).join('')}<div class="warning-strip">⚠ ${sceneText(L('Erase all content and settings','مسح المحتوى والإعدادات'))}</div></div>`;
  if(v.type==='cloud-share') return `<div class="scene realistic-device cloud-real"><div class="scene-top"><span>☁</span><strong>${appName}</strong><span>⋯</span></div><div class="share-from"><b>${sceneText(v.sender||'')}</b><small>${sceneText(v.file||'')}</small></div><div class="cloud-prompt">${sceneText(v.prompt||L('Sign in again','سجل الدخول مجددًا'))}</div><div class="fake-action">${sceneText(L('Open in app','فتح في التطبيق'))}</div></div>`;
  return `<div class="scene realistic-device browser-real"><div class="scene-top"><span>● ● ●</span><strong>${appName}</strong><span>⌕</span></div><div class="urlbar">🔒 https://example.com</div><div class="generic-card"><div class="fake-logo">◉</div><h3>${sceneText(v.title||L('Continue','متابعة'))}</h3><p>${sceneText(v.helper||'')}</p></div></div>`;
}

function testQuestion(q,kind,total){
  return `<div class="test-wrap"><div class="card question-card"><div class="question-no">${esc(kind)} · ${state.index+1}/${total}</div><div class="progress"><span style="width:${((state.index)/total)*100}%"></span></div><h2 class="question">${esc(q.prompt)}</h2><div class="choices">${(Array.isArray(q.choices)?q.choices:[]).map((c,i)=>`<label class="choice"><input type="radio" name="answer" value="${String.fromCharCode(97+i)}"><span>${esc(c)}</span></label>`).join('')}</div><div class="cta"><button class="btn btn-primary" id="nextBtn">${esc(state.index===total-1?t('finish'):t('next'))}</button></div><div id="err" class="notice hidden"></div></div></div>`;
}
function initial(){const q=state.questions[state.index];shell(testQuestion(q,t('initial'),state.questions.length));document.querySelectorAll('.choice').forEach(x=>x.onclick=()=>x.classList.add('selected'));document.getElementById('nextBtn').onclick=()=>{const a=document.querySelector('input[name=answer]:checked');if(!a){const e=document.getElementById('err');e.textContent=state.lang==='ar'?'اختر إجابة للمتابعة.':'Choose an answer to continue.';e.classList.remove('hidden');return}state.answers[q.id]=a.value;flashSaved();if(state.index<state.questions.length-1){state.index++;render()}else submitInitial()}}
async function submitInitial(){const result=scoreLocal(state.questions,state.answers);state.player.initialScore=result.score;persist();navigate('initialResult')}
function initialResult(){shell(`<div class="card form-card result"><div class="eyebrow">${esc(t('initialScore'))}</div><div class="result-hero"><div class="score-ring" style="background:conic-gradient(#254768 0 ${state.player.initialScore*3.6}deg,#e6ebf0 ${state.player.initialScore*3.6}deg)"><div class="score">${state.player.initialScore}</div></div><div><h2 class="section-title">${state.player.initialScore}/100</h2><p class="section-subtitle">${esc(t('journey'))}</p></div></div><button class="btn btn-primary" id="goGame">${esc(t('start'))}</button></div>`);document.getElementById('goGame').onclick=()=>{loadGame()}}
async function loadGame(forceRender=false){state.game=CC_DATA.challenges.map(c=>({...c,completed:Boolean(state.player.gameState?.challengeResults?.[c.id])}));navigate('game',{force:forceRender,history:!forceRender})}
function game(){const completed=state.game.filter(x=>x.completed).length;const next=state.game.find(x=>!x.completed);if(!next){navigate('finalIntro');return}const level=next.level;const levelName={1:'Phishing 📩',2:'Scams & Impersonation 🎭',3:'Account Security 🔐',4:'Privacy & Digital Identity 🕵️'}[level];const choiceHtml=next.type==='inspect'?`<div class="evidence-grid">${next.choices.map((c,i)=>`<div class="evidence" data-i="${String.fromCharCode(97+i)}"><span>✓</span>${esc(c)}</div>`).join('')}</div>`:next.type==='order'?`<div class="order-list" id="orderList">${next.choices.map((c,i)=>`<div class="order-row" data-val="${String.fromCharCode(97+i)}"><span>${esc(c)}</span><div class="order-actions"><button class="mini up">↑</button><button class="mini down">↓</button></div></div>`).join('')}</div>`:`<div class="choices">${next.choices.map((c,i)=>`<label class="choice"><input type="radio" name="g" value="${String.fromCharCode(97+i)}"><span>${esc(c)}</span></label>`).join('')}</div>`;shell(`<div class="game-shell"><div class="game-head"><div><div class="level-pill">${esc(t('level'))} ${level} / ${Math.max(...CC_DATA.challenges.map(x=>x.level||1))}</div><h2 class="section-title">${esc(levelName)}</h2><p class="section-subtitle">${esc(next.prompt)}</p></div><div class="stat xp-stat"><div>${esc(t('xp'))}</div><strong>${state.player.xp}</strong></div></div><div class="progress"><span style="width:${(completed/CC_DATA.challenges.length)*100}%"></span></div><div class="card challenge-card"><div class="question-no">${esc(t('challenge'))} · ${completed+1} / ${CC_DATA.challenges.length}</div><h2 class="question">${esc(next.title)}</h2>${choiceHtml}<div class="cta"><button class="btn btn-primary" id="gameSubmit">${esc(t('submit'))}</button></div><div id="feedback" class="hidden"></div></div></div>`);bindGameChoice(next)}
function bindGameChoice(next){
  if(next.type==='inspect')document.querySelectorAll('.evidence').forEach(x=>x.onclick=()=>x.classList.toggle('selected'));
  if(next.type==='order')document.querySelectorAll('.order-row').forEach(row=>{row.querySelector('.up').onclick=()=>move(row,-1);row.querySelector('.down').onclick=()=>move(row,1)});
  document.querySelectorAll('.choice').forEach(x=>x.onclick=()=>x.classList.add('selected'));
  document.getElementById('gameSubmit').onclick=async()=>{
    let answer;
    if(next.type==='inspect')answer=[...document.querySelectorAll('.evidence.selected')].map(x=>x.dataset.i);
    else if(next.type==='order')answer=[...document.querySelectorAll('#orderList .order-row')].map(x=>x.dataset.val);
    else answer=document.querySelector('input[name=g]:checked')?.value;
    if(!answer || (Array.isArray(answer)&&answer.length===0)){alert(state.lang==='ar'?'اختر إجابة أولًا.':'Choose an answer first.');return}
    const correct=equalAnswerLocal(answer,next.answer);
    const xpGain=correct?Number(next.xp||0):Math.floor(Number(next.xp||0)*0.25);
    state.player.xp+=xpGain;state.player.rank=rankFromXPLocal(state.player.xp);if(next.badge&&!state.player.badges.includes(next.badge))state.player.badges.push(next.badge);
    state.player.gameProgress=(state.player.gameProgress||0)+1;state.player.gameState ||= {challengeResults:{}};state.player.gameState.challengeResults[next.id]={correct,xp:xpGain,at:new Date().toISOString()};persist();
    effect(correct?'correct':'wrong');
    const box=document.getElementById('feedback');box.className=`feedback-box ${correct?'ok':'bad'}`;
    box.innerHTML=`<div class="feedback-head"><strong>${esc(correct?t('correct'):t('wrong'))}</strong><span>+${xpGain} XP</span></div><p>${esc(next.explanation?.[state.lang]||next.explanation?.en||'')}</p><button class="btn ${correct?'btn-primary':'btn-secondary'}" id="afterGame">${esc(t('nextChallenge'))}</button>`;
    document.getElementById('gameSubmit').classList.add('hidden');
    document.getElementById('afterGame').onclick=()=>loadGame(true);
  }
}
function equalAnswerLocal(a,b){if(Array.isArray(b))return Array.isArray(a)&&JSON.stringify(a)===JSON.stringify(b);return a===b}
function rankFromXPLocal(xp){if(xp>=800)return 'Cyber Expert';if(xp>=550)return 'Cyber Guardian';if(xp>=300)return 'Cyber Defender';return 'Cyber Rookie'}
function scoreLocal(questions,answers){let total=0;for(const q of questions)if(equalAnswerLocal(answers[q.id],q.answer))total+=Number(q.weight||0);return {score:Math.max(0,Math.min(100,total))}}
function shuffleLocal(arr){const a=[...arr];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
function persist(){localStorage.setItem('cc_static_player',JSON.stringify(state.player));}
function move(row,delta){const list=row.parentElement;const rows=[...list.children];const i=rows.indexOf(row);const j=i+delta;if(j<0||j>=rows.length)return;list.insertBefore(row,delta<0?rows[j]:rows[j].nextSibling)}
function finalIntro(){shell(`<div class="card form-card"><div class="eyebrow">${esc(t('final'))}</div><h2 class="section-title">${esc(t('final'))}</h2><p class="section-subtitle">${esc(t('gameIntro'))}</p><div class="final-note">${CC_DATA.final.A.length} new situations • different set each retry</div><button class="btn btn-primary" id="beginFinal">${esc(t('continue'))}</button></div>`);document.getElementById('beginFinal').onclick=()=>loadFinal()}
async function loadFinal(){const setName=((state.player?.finalAttempt||0)%2===0)?'A':'B';state.finalAttempt=(state.player?.finalAttempt||0)+1;state.questions=CC_DATA.final[setName];state.answers={};state.index=0;navigate('final')}
function final(){const q=state.questions[state.index];shell(testQuestion(q,t('final'),state.questions.length));document.querySelectorAll('.choice').forEach(x=>x.onclick=()=>x.classList.add('selected'));document.getElementById('nextBtn').onclick=()=>{const a=document.querySelector('input[name=answer]:checked');if(!a)return alert(state.lang==='ar'?'اختر إجابة.':'Choose an answer.');state.answers[q.id]=a.value;flashSaved();if(state.index<state.questions.length-1){state.index++;render()}else submitFinal()}}
let finalSubmitting=false;async function submitFinal(){if(finalSubmitting)return;finalSubmitting=true;const result=scoreLocal(state.questions,state.answers);const initialScore=Number(state.player.initialScore||0);const improvement=result.score-initialScore;const passed=result.score>=70;state.player.finalScore=result.score;state.player.improvement=improvement;state.player.passed=passed;state.player.finalAttempt=state.finalAttempt||1;state.player.certificateId=passed?`CC-${new Date().getFullYear()}-${Math.random().toString(16).slice(2,8).toUpperCase()}`:null;state.player.completionDate=new Date().toISOString().slice(0,10);persist();state.finalResult={initialScore,score:result.score,improvement,passed,certificateId:state.player.certificateId,completionDate:state.player.completionDate,rank:state.player.rank};finalSubmitting=false;navigate('progress')}
function rankTitle(type){return type==='world'?t('worldChampion'):type==='continent'?t('continentChampion'):t('nationalChampion')}
function progress(){const r=state.finalResult;const improvement=r.improvement;const msg=r.passed?`<div class="unlock"><span>🎓</span><div><strong>${esc(t('certificateReady'))}</strong><div>${esc(r.certificateId)}</div></div></div>`:`<div class="notice">${esc(t('review'))}</div>`;shell(`<div class="card form-card result"><div class="eyebrow">${esc(t('progress'))}</div><h2 class="section-title">${esc(t('progress'))}</h2><div class="stat-grid"><div class="stat">${esc(t('before'))}<strong>${r.initialScore}/100</strong></div><div class="stat">${esc(t('after'))}<strong>${r.score}/100</strong></div><div class="stat">${esc(t('improvement'))}<strong class="${improvement>=0?'success':'danger'}">${improvement>=0?'+':''}${improvement}</strong></div><div class="stat">${esc(t('rank'))}<strong style="font-size:16px">${esc(r.rank)}</strong></div></div><div style="margin-top:18px">${msg}</div><div class="cta">${r.passed?`<button class="btn btn-gold" id="cert">${esc(t('download'))}</button>`:`<button class="btn btn-primary" id="retry">${esc(t('retry'))}</button>`}<button class="btn btn-secondary" id="rankBtn">🏆 ${esc(t('rankings'))}</button></div></div>`);if(r.passed)document.getElementById('cert').onclick=()=>{navigate('certificate')};else document.getElementById('retry').onclick=()=>loadFinal();document.getElementById('rankBtn').onclick=()=>{navigate('rankings')}}
function certificate(){const p=state.player;const c=p?.country||state.countries.find(x=>x.code===p?.country?.code)||state.countries.find(x=>x.code==='DZ');shell(`<div class="certificate-page"><div class="certificate-header"><div><div class="eyebrow">${esc(t('certificateReady'))}</div><h1 class="section-title">🎓 ${esc(t('certificateReady'))}</h1></div><div class="cert-pill">${esc(p.certificateId||'')}</div></div><div class="cert-card" id="certCard"><div class="cert-glow"></div><div class="cert-brand"><span class="shield-mini">🔒</span><div><b>CYBER CHALLENGE</b><small>Learn • Protect • Empower</small></div></div><div class="cert-title">CERTIFICATE</div><div class="cert-sub">OF COMPLETION</div><div class="cert-presented">${state.lang==='ar'?'تُمنح هذه الشهادة إلى':'THIS CERTIFICATE IS PROUDLY PRESENTED TO'}</div><div class="cert-name">${esc(`${p.firstName} ${p.lastName}`)}</div><div class="cert-desc">${state.lang==='ar'?'نظير إكمال برنامج Cyber Challenge للتوعية بالأمن السيبراني':'for successfully completing the Cyber Challenge cybersecurity awareness program'}</div><div class="cert-metrics"><div><span>${esc(t('before'))}</span><b>${p.initialScore}/100</b></div><div><span>${esc(t('after'))}</span><b>${p.finalScore}/100</b></div><div><span>${esc(t('improvement'))}</span><b class="positive">${p.improvement>=0?'+':''}${p.improvement}</b></div><div><span>${esc(t('rank'))}</span><b>${esc(p.rank)}</b></div></div><div class="cert-details"><div><span>${esc(t('country'))}</span><b>${flagImg(c)} ${esc(c?.[state.lang]||'')}</b></div><div><span>${esc(t('age'))}</span><b>${p.age}</b></div><div><span>${state.lang==='ar'?'التاريخ':'Completion Date'}</span><b>${esc(p.completionDate||'')}</b></div><div><span>${state.lang==='ar'?'معرّف الشهادة':'Certificate ID'}</span><b>${esc(p.certificateId||'')}</b></div></div><div class="cert-bottom"><div class="cert-sign"><em>Cyber Challenge</em><span>${esc(t('owner'))}</span></div><div class="cert-verify">${esc(t('verify'))}: ${esc(p.certificateId||'')}</div></div></div><div class="cta cert-actions"><button class="btn btn-gold" id="download">⬇ ${esc(t('download'))}</button><button class="btn btn-secondary" id="printCert">🖨 ${state.lang==='ar'?'طباعة / PDF':'Print / PDF'}</button><button class="btn btn-secondary" id="rankBtn">🏆 ${esc(t('rankings'))}</button></div></div>`);document.getElementById('download').onclick=downloadCertificatePNG;document.getElementById('printCert').onclick=()=>window.print();document.getElementById('rankBtn').onclick=()=>navigate('rankings')}
function downloadCertificatePNG(){const p=state.player;const W=1600,H=1100;const escXml=s=>String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');const ar=state.lang==='ar';const title=ar?'شهادة إتمام':'CERTIFICATE OF COMPLETION';const presented=ar?'تُمنح هذه الشهادة إلى':'THIS CERTIFICATE IS PROUDLY PRESENTED TO';const desc=ar?'نظير إكمال برنامج Cyber Challenge للتوعية بالأمن السيبراني':'for successfully completing the Cyber Challenge cybersecurity awareness program';const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${W}" height="${H}" viewBox="0 0 ${W} ${H}"><rect width="100%" height="100%" fill="#08182f"/><rect x="35" y="35" width="1530" height="1030" rx="28" fill="#f8f4ec" stroke="#d7a546" stroke-width="8"/><rect x="55" y="55" width="1490" height="990" rx="22" fill="none" stroke="#10294a" stroke-width="3"/><text x="800" y="140" text-anchor="middle" font-family="Arial, sans-serif" font-size="68" font-weight="700" fill="#10294a" letter-spacing="8">${escXml(title)}</text><text x="800" y="250" text-anchor="middle" font-family="Georgia,serif" font-size="24" fill="#9b6b22" letter-spacing="6">${escXml(presented)}</text><text x="800" y="390" text-anchor="middle" font-family="Georgia,serif" font-size="76" font-style="italic" fill="#0b1d39">${escXml(p.firstName+' '+p.lastName)}</text><line x1="380" y1="420" x2="1220" y2="420" stroke="#d7a546" stroke-width="3"/><text x="800" y="480" text-anchor="middle" font-family="Arial,sans-serif" font-size="26" fill="#26344d">${escXml(desc)}</text><text x="800" y="525" text-anchor="middle" font-family="Arial,sans-serif" font-size="26" fill="#26344d">Cyber Challenge</text><g font-family="Arial,sans-serif" fill="#10294a"><text x="360" y="650" text-anchor="middle" font-size="20">${escXml(ar?'النتيجة الأولية':'INITIAL SCORE')}</text><text x="360" y="700" text-anchor="middle" font-size="42" font-weight="700">${p.initialScore}/100</text><text x="640" y="650" text-anchor="middle" font-size="20">${escXml(ar?'النتيجة النهائية':'FINAL SCORE')}</text><text x="640" y="700" text-anchor="middle" font-size="42" font-weight="700">${p.finalScore}/100</text><text x="960" y="650" text-anchor="middle" font-size="20">${escXml(ar?'التحسن':'IMPROVEMENT')}</text><text x="960" y="700" text-anchor="middle" font-size="42" font-weight="700">${p.improvement>=0?'+':''}${p.improvement}</text><text x="1240" y="650" text-anchor="middle" font-size="20">${escXml(ar?'الرتبة':'RANK')}</text><text x="1240" y="700" text-anchor="middle" font-size="30" font-weight="700">${escXml(p.rank)}</text></g><rect x="250" y="780" width="1100" height="130" rx="18" fill="#eef1f4" stroke="#c7a35c"/><text x="300" y="825" font-family="Arial,sans-serif" font-size="20" fill="#546070">${escXml(ar?'معرّف الشهادة':'CERTIFICATE ID')}</text><text x="300" y="865" font-family="Arial,sans-serif" font-size="30" font-weight="700" fill="#10294a">${escXml(p.certificateId||'')}</text><text x="820" y="825" font-family="Arial,sans-serif" font-size="20" fill="#546070">${escXml(ar?'الدولة':'COUNTRY')}</text><text x="820" y="865" font-family="Arial,sans-serif" font-size="30" font-weight="700" fill="#10294a">${escXml(p.country?.[state.lang]||'')}</text><text x="1220" y="825" font-family="Arial,sans-serif" font-size="20" fill="#546070">${escXml(ar?'العمر':'AGE')}</text><text x="1220" y="865" font-family="Arial,sans-serif" font-size="30" font-weight="700" fill="#10294a">${p.age}</text><text x="800" y="985" text-anchor="middle" font-family="Georgia,serif" font-size="28" font-style="italic" fill="#10294a">Cyber Challenge • ${escXml(ar?'المالك والمؤسس: دوبة جواد':'Owned &amp; Founded by Douba Jouad')}</text></svg>`;const blob=new Blob([svg],{type:'image/svg+xml;charset=utf-8'});const url=URL.createObjectURL(blob);const img=new Image();img.onload=()=>{const canvas=document.createElement('canvas');canvas.width=W;canvas.height=H;const ctx=canvas.getContext('2d');ctx.drawImage(img,0,0);URL.revokeObjectURL(url);canvas.toBlob(b=>{const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=`Cyber-Challenge-${p.certificateId||'certificate'}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)},'image/png')};img.src=url}
function rankingTitleFor(entry,type){if(entry.position===1)return rankTitle(type);return type==='world'?t('worldElite'):type==='continent'?t('continentElite'):t('nationalElite')}
function rankingCard(title,arr,type){return `<div class="rank-card"><div class="rank-card-head"><h3>${esc(title)}</h3><span>TOP 3</span></div>${arr.length?arr.map(x=>`<div class="rank-row ${x.position===1?'first':''}"><div class="rank-pos">${x.position===1?'👑':('#'+x.position)}</div><div class="rank-person"><b>${esc(x.name)}</b><small>${flagImg(x.country)} ${esc(x.country[state.lang])} · ${x.xp} XP</small></div><div class="rank-title">${esc(rankingTitleFor(x,type))}</div></div>`).join(''):`<div class="empty-rank">${state.lang==='ar'?'لا توجد نتائج مكتملة بعد':'No completed players yet'}</div>`}</div>`}
async function rankings(){shell(`<div class="rankings-page"><div class="rankings-hero"><div><div class="eyebrow">🏆 ${esc(t('rankings'))}</div><h1 class="section-title">${esc(t('rankings'))}</h1><p class="section-subtitle">${esc(t('live'))}</p></div><div class="live-dot">● LIVE</div></div><div id="rankingsGrid" class="rankings-grid"></div></div>`);renderRankings(localRankingData())}
function localRankingData(){
  const savedRaw=localStorage.getItem('cc_static_ranking')||'[]';
  let saved=[]; try{saved=JSON.parse(savedRaw);if(!Array.isArray(saved))saved=[]}catch{saved=[]}
  if(state.player?.finalScore!=null){
    const key=state.player.id||'me';
    const row={id:key,name:`${state.player.firstName?.[0]||''}••• ${state.player.lastName?.[0]||''}•••`,xp:Number(state.player.xp||0),finalScore:Number(state.player.finalScore||0),country:state.player.country};
    const i=saved.findIndex(x=>x.id===key); if(i>=0)saved[i]=row; else saved.push(row);
    localStorage.setItem('cc_static_ranking',JSON.stringify(saved));
  }
  saved.sort((a,b)=>(b.xp-a.xp)||(b.finalScore-a.finalScore));
  const top=saved.slice(0,3).map((x,i)=>({...x,position:i+1}));
  const continent=state.player?.country?.continent||'Local';
  const code=state.player?.country?.code||'DZ';
  return {world:top,continents:{[continent]:top},countries:{[code]:top}};
}

async function resume(){await loadCountries();try{const saved=JSON.parse(localStorage.getItem('cc_static_player')||'null');if(saved){state.player=saved;if(saved.finalScore!==null){state.finalResult={initialScore:saved.initialScore,score:saved.finalScore,improvement:saved.improvement,passed:saved.passed,certificateId:saved.certificateId,completionDate:saved.completionDate,rank:saved.rank};state.page=saved.passed?'certificate':'progress'}else if(saved.initialScore!==null){state.questions=CC_DATA.initial[state.lang]||CC_DATA.initial.en;state.page='initialResult'}else state.page='info'}}catch{}render()}
function render(){({home,info,initial,initialResult,game,finalIntro,final,progress,certificate,rankings}[state.page]||home)()}
resume();
